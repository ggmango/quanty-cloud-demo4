spring:
  profiles:
    active: dev
  application:
    name: quanty-cloud-service-quantyuzk
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml

#  main:
#    allow-bean-definition-overriding: true